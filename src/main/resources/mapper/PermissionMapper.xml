<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.vuecms1.mapper.PermissionMapper">

    <!-- 通过权限编码检查用户是否有某个权限 -->
    <select id="hasPermissionByCode" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user_role ur
                 INNER JOIN role_permission rp ON ur.role_id = rp.role_id
                 INNER JOIN permission p ON rp.permission_id = p.id
        WHERE ur.user_id = #{userId}
          AND p.code = #{permissionCode}
    </select>

    <!-- 获取用户所有权限编码 -->
    <select id="getUserPermissionCodes" resultType="string">
        SELECT DISTINCT p.code
        FROM user_role ur
                 INNER JOIN role_permission rp ON ur.role_id = rp.role_id
                 INNER JOIN permission p ON rp.permission_id = p.id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>