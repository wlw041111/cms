<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.vuecms1.mapper.RoleMapper">

    <resultMap id="PermissionResultMap" type="org.example.vuecms1.entity.Permission">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="menuType" column="type"/>
        <result property="code" column="code"/>
        <result property="path" column="path"/>
        <result property="parentId" column="parent_id"/>
        <result property="sort" column="sort"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="getRoleInfoPage" resultType="org.example.vuecms1.entity.Role">
        SELECT *
        FROM role
    </select>

    <select id="selectPermissionList" resultMap="PermissionResultMap">
        SELECT id,
               name,
               type,
               code,
               path,
               parent_id,
               sort,
               create_time
        FROM cms.permission
        ORDER BY parent_id ASC, sort ASC, id ASC
    </select>

    <select id="selectPermissionIdsByRoleId" resultType="java.lang.Integer">
        SELECT permission_id
        FROM cms.role_permission
        WHERE role_id = #{roleId}
    </select>

    <delete id="deleteRolePermissions">
        DELETE
        FROM cms.role_permission
        WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRolePermissions">
        INSERT INTO cms.role_permission (role_id, permission_id)
        VALUES
        <foreach collection="permissionIds" item="permissionId" separator=",">
            (#{roleId}, #{permissionId})
        </foreach>
    </insert>

    <!-- 通过权限编码检查用户是否有某个权限 -->
    <select id="hasPermissionByCode" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user_role ur
                 INNER JOIN role_permission rp ON ur.role_id = rp.role_id
                 INNER JOIN permission p ON rp.permission_id = p.id
        WHERE ur.user_id = #{userId}
          AND p.code = #{permissionCode}
    </select>

    <!-- 获取用户所有权限编码 -->
    <select id="getUserPermissionCodes" resultType="string">
        SELECT DISTINCT p.code
        FROM user_role ur
                 INNER JOIN role_permission rp ON ur.role_id = rp.role_id
                 INNER JOIN permission p ON rp.permission_id = p.id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>